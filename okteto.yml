build:
  users-ms:
    context: .
    dockerfile: Dockerfile

    
deploy:
  compose:
    file: docker-compose.yml

dev:
  users-ms:
    forward:
      - 3000:3000
      - 5432:users-db:5432
    sync:
      - .:/usr/snapmsg-users

  dd-agent:
    forward:
      - 3000:users-ms:3000

